/** @deprecated Prefer `@/data/stocks` — mantido para imports legados. */
export type { SectorRecord, SectorSlug } from "@/data/stocks";
export {
  SECTORS,
  SECTOR_SLUGS,
  getSector,
  getSectorPath,
  getStocksInSector,
} from "@/data/stocks";
